{
  "scripts": {
    "start": "docker container stop $(docker ps -q); docker-compose up --force-recreate --build",
    "import-production-database": "sh development/scripts/import-production-database.sh"
  }
}
